# Task-Management-APP
The project includes:  Backend APIs using Laravel with authentication, task management, role-based access control, and validations.  Frontend implementation using React with TypeScript, including authentication, task listing, filters, and admin dashboard features.
